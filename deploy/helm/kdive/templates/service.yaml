apiVersion: v1
kind: Service
metadata:
  name: {{ include "kdive.fullname" . }}-server
  labels:
    {{- include "kdive.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ include "kdive.fullname" . }}-server
  ports:
    - port: 8000
      targetPort: 8000
